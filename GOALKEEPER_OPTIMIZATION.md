# 蓝队守门员优化说明

本补丁只修改实际启用的蓝队包 `brain_blue_wangyifei_v1`，未改动红队和蓝队其他角色策略。

## 已确认的原逻辑问题

1. 守门员已有球门前站位和横移边界，但只根据球的当前位置计算封堵点。
2. 没有球速估计和门线交点预测，快速射门与慢球共用追球逻辑。
3. 正常比赛出击距离为 3.8 m，容易被慢球或横向球带离门前。
4. 近球阶段缺少清晰的进入/退出滞回，容易在追球、调整和踢球之间碎步切换。
5. 门前转向速度按 `ball_yaw * 4` 直接输出，节点内没有限幅。

## 本次修改

- 对新鲜的球位置采样做有限差分和低通滤波，拒绝超过 9 m/s 的定位跳变。
- 预测球到己方门线的时间和交点；预计 2.6 秒内进入球门范围时进入 `block` 状态。
- `block` 状态不追球，守门员留在距门线约 0.55 m 的平面上，横移至预测拦截点。
- 普通出击阈值由 3.8 m 收紧为 2.4 m，并要求球真正进入门区或近距离禁区走廊。
- 球明显远离己方球门时不再追出，优先回到封堵位置。
- 追球退出距离增加 0.45 m 滞回；近球处理使用 0.72 m / 0.95 m 的进入、退出阈值。
- 踢球前要求守门员位于球的球门侧，降低从错误方向解围造成乌龙的风险。
- 守位和封堵的角速度限制为 0.85–0.90 rad/s，降低急转和摔倒概率。

## 仿真时重点观察

- 快速射向蓝队球门的球出现时，决策标记应显示 `goalie-block`。
- 远处慢球、横向球或向前场滚动的球不应再轻易把守门员带出禁区深处。
- 守门员应主要沿门前横移，转身动作比原版本平缓。
- 球停在门区且守门员足够近时，流程应为 `chase -> adjust/kick -> retreat`。
- 若守门员仍在近球时碎步，可优先微调 XML 中的 `clear_enter_range` 和 `clear_exit_range`。

## 可调参数

参数位于：

`src/brain_blue_wangyifei_v1/behavior_trees/subtrees/subtree_goal_keeper_play.xml`

- `chase_threshold`：首次允许出击的最大球距，当前 2.40 m。
- `chase_exit_margin`：已经出击后的退出滞回，当前 0.45 m。
- `clear_enter_range`：从追球进入近球处理的距离，当前 0.72 m。
- `clear_exit_range`：近球处理退出距离，当前 0.95 m。
- `shot_min_speed`：判定球正射向己方球门的最小 X 向速度，当前 0.20 m/s。
- `shot_max_time`：启用门线预测的最长到达时间，当前 2.60 s。

## 验证说明

补丁生成环境未安装 ROS2 Humble，无法在生成端执行最终 `colcon build`。XML 结构、Shell 脚本语法和修改范围已做静态检查；请在 Ubuntu 22.04 / WSL 的 ROS2 Humble 环境中按项目脚本重新编译。
